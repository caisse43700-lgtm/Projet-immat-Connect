# AUDIT ARCHITECTURAL — ImmatConnect
> Date : 2026-06-02
> Périmètre : UX-Architecture (10 fichiers), NS JSON/TS, brain-dialog, index.html
> Méthode : Exploration statique + vérification croisée documentation ↔ code

---

## RÉSUMÉ EXÉCUTIF

L'architecture est bien pensée mais présente **3 bugs fonctionnels réels** et **2 incohérences documentaires**. Aucun refactoring majeur requis — corrections chirurgicales suffisent.

---

## BUGS FONCTIONNELS (impact utilisateur réel)

### BUG-001 — quickMsg() et quickReply() : champs morts après migration
- **Fichier** : index.html lignes 699, 790
- **Gravité** : Haute
- **Symptôme** : Le panneau Messages s'ouvre mais le compose est vide. Le message tapé dans la réponse rapide (`t`) est perdu.
- **Cause** : Ces fonctions écrivent dans `$('iMsg')` et `$('iTarget')` — champs de `panelContact` (caché). L'install() de compatibilité (ligne 1606) redirige `panel('contact')` → `panel('messages')`, mais ne transfère pas les données.
- **Contexte** : `vehicleAlert()` et `pickPlate()` ont déjà été migrés par l'install block. `quickMsg`/`quickReply` ne l'ont pas été.
- **Fix** : Réécrire ces 2 fonctions pour cibler `icComposePlate` / `icComposeText` + appeler `ImmatMessages.setMode('compose')`.
- **Statut** : ✅ CORRIGÉ dans ce commit

### BUG-002 — Debug tools visibles de tous les utilisateurs
- **Fichier** : index.html ligne 214 (panelSettings)
- **Gravité** : Haute (FRI-004, D-007)
- **Symptôme** : "📨 Restaurer msgs" et "🔄 Sync alertes" apparaissent dans les paramètres de tous les conducteurs. Confusion, sentiment d'interface cassée.
- **Cause** : Boutons non conditionnés au rôle Gardien.
- **Fix** : CSS `display:none` + classe `gardien-debug-tool`, révélés quand `S.isGardien = true`.
- **Statut** : ✅ CORRIGÉ dans ce commit

### BUG-003 — brain-dialog : pas de retry sur erreur RPC rôle
- **Fichier** : supabase/functions/immat-brain-dialog/index.ts ligne 220
- **Gravité** : Moyenne
- **Symptôme** : Si `get_my_role()` échoue en cold start (Supabase lent), le Gardien reçoit un 403 et doit ré-essayer manuellement.
- **Cause** : `Promise.all([auth, rpc])` — si rpc échoue, return 403 immédiat, aucun retry.
- **Fix** : 1 retry avec 600ms backoff si `roleErr`.
- **Statut** : ✅ CORRIGÉ dans ce commit

---

## INCOHÉRENCES ARCHITECTURE ↔ CODE

### INC-001 — panelContact : condition D-001 ✅ REMPLIE (SESSION 11)
- **Décision** : D-001 "Supprimer panelContact après confirmation que 0 accès directs subsistent"
- **SESSION 11** : Après analyse complète des call sites :
  - `voicePlate()` et `voiceMsg()` migrés → `icComposePlate`/`icComposeText`
  - Fallbacks `vehicleAlertQuick` et `actQuickReply` migrés → `panel('messages')` + `ImmatMessages`
  - `'contact'` retiré de la liste `panel()` routing
  - `div#panelContact` supprimé du DOM
  - Références résiduelles `if($('iTarget'))` dans pickPlate/showVehicleContextMenu/vehicleContextAction : silencieusement inoffensives (retournent null → if guard → no-op)
- **Statut** : ✅ FAIT (SESSION 11)

### INC-002 — UX-BACKLOG statuts décalés
- **P1-003** : "Supprimer onglet Nouveau — ✅ fait (SESSION 8)" — Correct (supprimé du DOM). Mais le code JS `navSignaler` et la logique `actCatTab` subsistent en code mort.
- **Impact** : Cosmétique — le code mort ne cause pas de bug. Nettoyage possible à P2.

---

## DETTE TECHNIQUE (pas de bug, à surveiller)

### DET-001 — Références lignes index.html dans NS
- `immat-nervous-system.json` documente des entry points par numéro de ligne (ex: `"afterAuth": "index.html:507"`). Si des lignes sont ajoutées, ces références dérivent silencieusement.
- **Recommandation** : Remplacer par des anchors symboliques plutôt que numéros de ligne, ou ajouter un linter `scripts/validate-ns-refs.js`.

### DET-002 — nsToPrompt() : pas de validation schéma NS au démarrage ✅ TRAITÉ (SESSION 11)
- Si NS change de structure (renommage d'un champ), `nsToPrompt()` échouait silencieusement (retournait un prompt incomplet) plutôt que de crasher avec une erreur claire.
- **Fix SESSION 11** : `validateNSSchema()` ajoutée dans `brain-dialog/index.ts`. Vérifie 5 champs obligatoires (`organs`, `routing`, `inhibitions`, `invariants`, `ange_identity`) avec messages d'erreur explicites par champ. Appelée avant `nsToPrompt(3)` au cold start.
- **Statut** : ✅ FAIT (SESSION 11)

### DET-003 — Cross-références UX architecture faibles
- UX-BUTTONS.json, UX-INTERACTIONS.json, UX-JOURNEYS.json, UX-FRICTIONS.json et UX-DECISIONS.md coexistent sans liens bidirectionnels entre leurs IDs (BTN-*, INT-*, JRN-*, FRI-*, D-*).
- **Recommandation** : Ajouter des champs `"frictions": ["FRI-001"]` dans les interactions, `"interactions": ["INT-002"]` dans les boutons, etc. Rend la navigation entre fichiers possible.

---

## CE QUI FONCTIONNE BIEN ✅

1. **sync-ns.js** — impeccable : génère TS depuis JSON, mode --check pour CI, INV-015 respecté
2. **NS v6** — structure cohérente avec access_policy 3 rôles
3. **nsToPrompt(depth)** — projection correcte par profondeur
4. **install() block** — redirige intelligemment `panel('contact')` → `messages` + patch vehicleAlert/pickPlate
5. **Architecture UX modulaire** — 10 fichiers, 2275 lignes, couverture complète
6. **SOS appui long** — D-005 bien implémenté (long-sos + pointerdown/up)
7. **STATIC_SYSTEM caché Anthropic** — prompt_caching correctement appliqué
8. **Promise.all auth + rôle** — parallélisme bien pensé (avant fix retry)
9. **validateOutput()** — fix sources/juste contradictoires correct

---

## PLAN DE CORRECTION PRIORISÉ

| ID | Fix | Effort | Statut |
|---|---|---|---|
| BUG-001 | quickMsg/quickReply → icComposePlate/icComposeText | Faible | ✅ fait |
| BUG-002 | Debug tools → Gardien seulement | Faible | ✅ fait |
| BUG-003 | brain-dialog retry RPC | Très faible | ✅ fait |
| INC-001 | panelContact DOM suppression | Moyen | ✅ fait (SESSION 11) |
| DET-001 | NS refs lines → anchors symboliques | Moyen | 🔲 futur |
| DET-002 | nsToPrompt fail-fast schema validation | Faible | ✅ fait (SESSION 11) |
| DET-003 | Cross-refs bidirectionnelles UX docs | Faible | 🔲 futur |
